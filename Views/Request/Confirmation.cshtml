@{
    ViewData["Title"] = "Request Confirmation";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8 text-center">
            <div class="card border-success">
                <div class="card-body">
                    <h1 class="text-success mb-4">✓ Request Submitted Successfully</h1>
                    
                    <div class="alert alert-success">
                        <h4 class="alert-heading">Thank you for your request!</h4>
                        <p class="mb-0">Your equipment request has been received and someone from the IT department will be in touch soon.</p>
                    </div>

                    <div class="mt-4">
                        <a href="@Url.Action("Index", "Home")" class="btn btn-primary me-2">Back to Home</a>
                        <a href="@Url.Action("RequestForm", "Request")" class="btn btn-outline-primary">Make Another Request</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>